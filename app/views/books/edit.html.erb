<h1>Editing Book</h1>
<%= form_with model: @book, local:true do |f| %>
  <p>Title</p>
  <%= f.text_field :title, class: "form" %>
  <p>Body</p>
  <%= f.text_area :body, class: "form" %>

  <%= f.submit 'Update Book', class: "submit-button", method: :update  %>
<% end %>